[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v -ra --tb=short
markers =
    smoke: Smoke tests for critical path verification
    regression: Full regression test suite
    auth: Authentication and session management tests
    inventory: Inventory management and CRUD tests
